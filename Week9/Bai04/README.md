# Bai 04 - Matrix Strategy

Workflow chay test song song tren:

- `ubuntu-latest`
- `windows-latest`
- `macos-latest`

Loi co y ban dau co the tao bang cach assert chuoi `"data\\reports\\summary.txt"` hoac `"data/reports/summary.txt"`. Cach sua dung la dung `Path.of("data", "reports", "summary.txt")`, nhu test hien tai.
